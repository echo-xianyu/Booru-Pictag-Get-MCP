# ⚠️ 纯 AI 生成声明 | Pure AI-Generated Notice

> **本项目代码由 AI（opencode + GLM-5.2）几乎完全生成，未经人工编写。**
>
> **This project's code is almost entirely generated by AI (opencode + GLM-5.2), without human-written code.**

人工仅介入以下环节（需求设计、方案讨论、测试与验收、本声明撰写）：
- 与 AI 协作确定项目定位、工具边界、命名；
- 人工把关默认管线决策（tag-conflicts 默认不开启、`optimize_tags` 多主体守门等）；
- 提供真实的 booru API key 进行端到端联网测试；
- 审阅并确认所有最终代码与文档。

Human involvement was limited to: requirement & naming decisions, default-pipeline guardrail decisions (tag-conflicts off-by-default, multi-subject guard in `optimize_tags`), providing real booru API credentials for end-to-end testing, and final review/acceptance of the AI-generated code and docs.

---

## 关于源项目 / About the upstream

本仓库是 Mexes-GM 的 [`booru-prompt-gallery`](https://github.com/Mexes-GM/booru-prompt-gallery) 项目的**移植 + 重写 + MCP 化衍生作品**，并非 fork。

- **源项目**：[https://github.com/Mexes-GM/booru-prompt-gallery](https://github.com/Mexes-GM/booru-prompt-gallery)
  - 语言：TypeScript + Next.js 15（Web 应用）
  - 作者：Mexes-GM
  - 协议：MIT
  - 价值：原创的 prompt 清洗管线（`cleanPrompt.ts` + `tag-classifier.ts` + `tag-conflicts.ts` 180+ 条单主体互斥规则 + `background-detector.ts` 等）

- **本项目**：[https://github.com/echo-xianyu/Booru-Pictag-Get-MCP](https://github.com/echo-xianyu/Booru-Pictag-Get-MCP)
  - 语言：Python 3（FastMCP 服务器）
  - 形态：MCP (Model Context Protocol) stdio 工具集，无 Web UI / 无 Supabase / 无 Cloudflare Worker
  - 移植范围：`cleanPrompt.ts`、`tag-classifier.ts`、`tag-conflicts.ts`、`background-detector.ts`/`background-dictionary.ts`、`color-theory.ts`、`post-transformers.ts`、`constants.ts` 等纯逻辑模块 1:1 翻译成 Python (`booru_pictag_get_mcp/core/*`)
  - 新增：FastMCP stdio 服务器（4 个 tool）、`autocomplete_tags`、5-provider 统一接入、本地静态 tag 分类字典（取代原项目对 Supabase 的依赖）

移植行为在原项目 MIT 协议下被明确允许，仅需保留原版权与许可声明——见根目录 [`LICENSE`](./LICENSE)（双重署名）。

This repository is a **port + rewrite + MCP integration** of Mexes-GM's [`booru-prompt-gallery`](https://github.com/Mexes-GM/booru-prompt-gallery), not a fork.

Porting the prompt-cleaning pipeline to Python and wrapping it as a FastMCP stdio server was performed under the upstream project's MIT license, which permits derivative works including language ports. The original copyright & permission notice is preserved in [`LICENSE`](./LICENSE) (dual attribution).